<doc>
  <head>
    <title>Mibble Installation Instructions</title>
  </head>

  <body>

    <h1>Requirements</h1>

    <p>The following requirements must be met in order to use
    Mibble:</p>

    <list>
      <item>
        <text>Java runtime compatible with JDK 1.4.</text>
      </item>
    </list>


    <h1>Installation</h1>

    <p>This software does not require any special installation. The
    only files needed are the jar-files in the <code>lib</code>
    subdirectory.</p>


    <h1>Running</h1>

    <p>Mibble is a library, but provides a simple MIB file browser
    application that can be used for testing purposes. It can be run
    by double-clicking the <code>MibbleBrowser.jar</code> file that
    is found in the base directory.</p>

    <p>There are also a few other test tools that can be run from the
    command-line or via the following scripts:</p>

    <p><code>bin/MibbleBrowser.sh</code> or 
    <code>bin/MibbleBrowser.bat</code></p>

    <p><code>bin/MibblePrinter.sh</code> or 
    <code>bin/MibblePrinter.bat</code></p>

    <p><code>bin/MibbleValidator.sh</code> or 
    <code>bin/MibbleValidator.bat</code></p>

    <p>The above scripts should find and set the Java
    <code>CLASSPATH</code> to the jar-files in the <code>lib</code>
    subdirectory:</p>

    <list>
      <item>
        <text><code>mibble-parser-<param name="version"/>.jar</code></text>
      </item>

      <item>
        <text><code>mibble-mibs-<param name="version"/>.jar</code></text>
      </item>

      <item>
        <text><code>snmp6_0.jar</code> (only required by
        MibbleBrowser)</text>
      </item>
    </list>

  </body>
</doc>
