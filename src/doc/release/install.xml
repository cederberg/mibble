<doc>
  <head>
    <title>Mibble Installation Instructions</title>
  </head>

  <body>

    <h1>Requirements</h1>

    <p>The following requirements must be met in order to use 
    Mibble:</p>

    <list>
      <item>
        <text>Java runtime compatible with JDK 1.3.</text>
      </item>
    </list>


    <h1>Installation</h1>

    <p>This software does not require any special installation. The only
    files needed are the <code>mibble-<param name="version"/>.jar</code> 
    and <code>grammatica-1.2.jar</code> from the <code>lib</code>
    subdirectory.</p>


    <h1>Running</h1>

    <p>Mibble is a library, and has no main application in this release.
    There are two test applications, however, a MIB file validator and a
    MIB file pretty-printer. These tools can be accessed with the 
    commands:</p>

    <pre>java net.percederberg.mibble.MibbleValidator</pre>

    <pre>java net.percederberg.mibble.MibblePrinter</pre>

    <p>Both of the above commands require that the <code>CLASSPATH</code>
    contains the two JAR files in the <code>lib</code> subdirectory:</p>

    <list>
      <item>
        <text><code>mibble-<param name="version"/>.jar</code></text>
      </item>

      <item>
        <text><code>grammatica-1.2.jar</code></text>
      </item>
    </list>

  </body>
</doc>
